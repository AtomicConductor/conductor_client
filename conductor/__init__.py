import conductor.lib.common

# Create a top level config
CONFIG = conductor.lib.common.Config().config

# Use this logger object for all conductor logging needs
# Ex: conductor.logger.info("this is an info message")
logger = conductor.lib.common.LOGGER

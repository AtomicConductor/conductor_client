import os


def mkdir_p(path):
    try:
        os.makedirs(path)
    except OSError as ex:
        if ex.errno == errno.EEXIST and os.path.isdir(path):
            pass
        else:
            raise


mkdir_p("/Users/julian/projects/fish/clarisse_fish/conductor")

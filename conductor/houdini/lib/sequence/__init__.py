"""The sequence package contains modules for dealing with frame numbers.

sequence: a clumpable sequence of frames.
clump: a regular or irregular set of frames.
progressions: functions to create arithmetic progressions.

These files are collected into this package because the are
required by the chrender.py script. In the UI the user
can select the script and these files to upload.
"""

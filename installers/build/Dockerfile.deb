FROM debian:stretch

RUN dpkg --add-architecture i386 &&\
    apt-get update &&\
    apt-get install -y \
      cpio \
      libssl1.0-dev \
      zlib1g-dev \
      libxml2-dev \
      libc6:i386 \
      libstdc++6:i386